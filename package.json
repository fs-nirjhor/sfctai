{
  "name": "sfctai",
  "version": "1.0.0",
  "description": "SFCTAI is a cryptocurrency trading platform",
  "main": "server.js",
  "keywords": [
    "sfctai",
    "SFCTAI",
    "sfctai.com",
    "trading",
    "trading platform",
    "cryptocurrency",
    "crypto",
    "coin",
    "income",
    "earning",
    "invest"
  ],
  "scripts": {
    "client-start": "cd client && npm start",
    "client-start-dev": "cd client && npm run dev",
    "client-build": "cd client && npm run build",
    "server-start": "cd server && npm start",
    "server-start-dev": "cd server && npm run dev",
    "server-build": "cd server && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "nodemon server/src/server.js",
    "start": "node server/src/server.js",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix server && npm install --legacy-peer-deps --prefix client && npm run build --prefix client"
  },
  "author": "sfctai",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.0.2"
  }
}
